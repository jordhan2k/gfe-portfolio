# name: Deploy Marketing app to Vercel

# on:
#   push:
#     branches:
#       - main
#     paths:
#       - "apps/gfe-marketing/**"
#       - "packages/**"
#       - "package.json"
#       - "package-lock.json"
#   pull_request:
#     branches:
#       - main
#     paths:
#       - "apps/gfe-marketing/**"
#       - "packages/**"
#       - "package.json"
#       - "package-lock.json"

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: "npm"

#       - name: Install dependencies
#         run: npm ci

#       - name: Build Turborepo
#         run: npm run build -w=gfe-marketing

#       - name: Deploy to Vercel
#         run: |
#           npx vercel --prod \
#             --cwd apps/web \
#             --token=${{ secrets.VERCEL_TOKEN }} \
#             --scope=${{ secrets.VERCEL_ORG_ID }} \
#             --confirm
#         env:
#           VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#           VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
#           VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
